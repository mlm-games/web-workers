[package]
edition = "2021"
name = "minimal-versions"
publish = false
version = "0.0.0"

[features]
audio-worklet = ["web-workers/audio-worklet"]
message = ["web-workers/message"]

[dependencies]
web-workers = { path = ".." }
